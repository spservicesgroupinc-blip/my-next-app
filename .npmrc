# Ensure npm always resolves packages from this project's node_modules.
# This prevents a stray package.json in a parent or home directory from
# interfering with module resolution when running npm scripts.
engine-strict=true
save-exact=true
